{
  "name": "brandon-social-network-api",
  "version": "1.0.0",
  "description": "This challenge uses NoSQL to create social networking api routes.",
  "main": "server.js",
  "scripts": {
  "test": "echo \"Error: no test specified\" && exit 1",
  "start": "node server.js",
  "seed": "node utils/seeds.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.3"
  }
}
